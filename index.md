# 刷题总索引（index.md）

本文件收录 `java/` 目录下全部题解，按题号升序排列，表格风格与 [bytedance-index.md](bytedance-index.md) 保持一致。每行包含：题目信息（题号、中文名称、英文名/Slug、题目关键词、题目概要描述）、本地解法信息（解法关键词、解法思路描述，均以本地实现代码为准核对）、Codetop 公司题频 Top 200 对照（排名与题频，抓取于 2026-08-23）。

共收录 **140** 道题的题解，其中 **124** 道进入 Codetop 公司题频 Top 200。数据源：`https://codetop.cc/api/questions/?company=1&page=1..10&ordering=-frequency`（未入 Top 200 的题目附全量榜题频）。公共数据结构定义见 [`java/common.java`](java/common.java)。

## 索引表

| 题号 | 中文名称 | 英文名/Slug | 题目关键词 | 题目概要描述 | 本地解法关键词 | 本地解法思路描述 | Codetop排名 | Codetop题频 | 本地文件 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 两数之和 | two-sum | 数组、哈希表、补数 | 在数组中找出和为 target 的两个数并返回下标。 | 数组、哈希表、补数、一次遍历、值映射下标 | 用 `HashMap<Integer,Integer> cache` 存「值->下标」，遍历时查 `target - nums[i]` 是否在表中，命中即返回两个下标，否则把当前元素 `<值,下标>` 入表。 | 第15名 | 126次 | [`java/1.java`](java/1.java) |
| 2 | 两数相加 | add-two-numbers | 链表、模拟、进位 | 两个逆序链表表示非负整数，返回它们的和。 | 链表、模拟、进位、链表模拟、进位加法、dummy 头节点 | `dummy` 头节点串接结果，`carry` 维护进位，逐位相加 `l1`、`l2` 后处理剩余链表与最终进位。 | 第61名 | 49次 | [`java/2.java`](java/2.java) |
| 3 | 无重复字符的最长子串 | longest-substring-without-repeating-characters | 字符串、滑动窗口、哈希计数 | 给定字符串，求不含重复字符的最长连续子串长度。 | 字符串、滑动窗口、哈希计数、字符计数 | 右指针扩张窗口，`cnt[128]` 统计字符出现次数，当 `cnt[c] > 1` 时左指针右移直到窗口无重复，更新最大窗口长度。 | 第1名 | 675次 | [`java/3.java`](java/3.java) |
| 4 | 寻找两个正序数组的中位数 | median-of-two-sorted-arrays | 数组、二分、分治、第 k 小 | 在两个有序数组中求整体中位数，要求 O(log(m+n))。 | 数组、二分、分治、第 k 小、二分查找、第k小、双数组 | 中位数转为求第 `k` 小，`getKthElement` 迭代（双下标 `index1`/`index2`）比较两数组 `k/2` 位置（不足取末尾）的 `pivot`，丢弃较小的一段；某数组耗尽直接返回另一数组第 k 个，`k==1` 时取两数组首元素较小值。 | 第37名 | 69次 | [`java/4.java`](java/4.java) |
| 5 | 最长回文子串 | longest-palindromic-substring | 字符串、回文、中心扩展、动态规划 | 返回字符串中最长的回文子串。 | 字符串、回文、中心扩展、动态规划、回文子串 | 枚举每个位置作为回文中心，分别对奇数长度 `(i,i)` 和偶数长度 `(i,i+1)` 调用 `expandAroundCenter` 向两侧扩展，记录最长回文起止位置。 | 第12名 | 135次 | [`java/5.java`](java/5.java) |
| 8 | 字符串转换整数 (atoi) | string-to-integer-atoi | 字符串、模拟、边界处理 | 按 atoi 规则将字符串转换为 32 位有符号整数。 | 字符串、模拟、边界处理、正则表达式、越界截断、Long 解析 | 用正则 `^\s*([+-]?\d+).*` 提取数字部分，`Long.parseLong` 解析后与 `Integer.MAX_VALUE`/`MIN_VALUE` 比较截断，解析失败按符号返回边界值。 | 第94名 | 29次 | [`java/8.java`](java/8.java) |
| 11 | 盛最多水的容器 | container-with-most-water | 数组、双指针 | n 条垂线高度 height，选两条使得与 x 轴构成的容器容纳最多水，返回最大容量。 | 数组、双指针、收缩较矮一侧 | 左右双指针 `i`、`j` 从两端出发，容器高度由较矮一侧决定：移动较高一侧不可能得到更大容量（宽度还在缩减），故每次移动较矮一侧指针向中间收缩，过程中用 `(j-i)*height[矮侧]` 更新 `max_vol`，相遇即止。 | 第103名 | 26次 | [`java/11.java`](java/11.java) |
| 14 | 最长公共前缀 | longest-common-prefix | 字符串 | 求字符串数组的最长公共前缀，不存在返回空串。 | 字符串、纵向扫描、逐列比较 | 以 `strs[0]` 为基准，`i` 表示当前公共前缀长度，逐列对比所有字符串同位置字符，某串到达末尾或字符不等即 `return s0.substring(0,i)`。 | 第115名 | 24次 | [`java/14.java`](java/14.java) |
| 15 | 三数之和 | 3sum | 数组、排序、双指针、去重 | 找出数组中所有和为 0 且不重复的三元组。 | 数组、排序、双指针、去重、三数之和 | 先 `Arrays.sort` 排序，固定 `i` 后用 `j`/`k` 双指针在剩余区间求两数之和，跳过重复元素，并用最小/最大两数和做剪枝。 | 第8名 | 185次 | [`java/15.java`](java/15.java) |
| 17 | 电话号码的字母组合 | letter-combinations-of-a-phone-number | 字符串、回溯 | 给定数字字符串，返回其能表示的所有字母组合（电话九宫格按键）。 | 回溯、DFS、路径数组 | `MAPPING[i]` 存数字 i 的字母集，`dfs(i)` 枚举当前位字母写入 `path[i]`，`i==n` 时把 `path` 组串加入 `ans`。 | 第171名 | 13次 | [`java/17.java`](java/17.java) |
| 19 | 删除链表的倒数第N个节点 | remove-nth-node-from-end-of-list | 链表、双指针、哑节点 | 删除链表倒数第 n 个节点并返回头节点。 | 链表、双指针、哑节点、快慢指针、dummy 哨兵 | `dummy` 哨兵简化头删，`right` 先走 `n` 步，再与 `left` 同步前进至 `right.next==null`，`left` 停在待删节点前驱，执行 `left.next=left.next.next`。 | 第55名 | 50次 | [`java/19.java`](java/19.java) |
| 20 | 有效的括号 | valid-parentheses | 字符串、栈、括号匹配 | 判断括号字符串是否按正确顺序闭合。 | 字符串、栈、括号匹配、哈希表 | `pairs` 映射右括号->左括号，遇左括号 `stack.push`，遇右括号检查栈顶是否等于 `pairs.get(ch)`，奇数长度直接返回 `false`，最后栈空即有效。 | 第24名 | 90次 | [`java/20.java`](java/20.java) |
| 21 | 合并两个有序链表 | merge-two-sorted-lists | 链表、双指针、递归 | 合并两个升序链表并返回新链表头。 | 链表、双指针、递归、迭代、哨兵节点 | `dummy` 哑节点 + `prev` 指针，比较 `l1.val <= l2.val` 把较小者接到 `prev.next` 并后移 `prev`，循环结束后把未合并完的链表直接拼接到 `prev.next`。 | 第29名 | 83次 | [`java/21.java`](java/21.java) |
| 22 | 括号生成 | generate-parentheses | 回溯、递归、卡特兰数 | 生成 n 对括号的所有合法组合。 | 回溯、递归、卡特兰数、DFS、括号生成 | `dfs` 回溯构建括号串，左括号数 `<max` 可加 `(`，右括号数 `<left` 可加 `)`，长度达 `2*max` 时加入 `ans`。 | 第41名 | 66次 | [`java/22.java`](java/22.java) |
| 23 | 合并K个排序链表 | merge-k-sorted-lists | 链表、归并、堆、分治 | 将 k 个升序链表合并为一个升序链表。 | 链表、归并、堆、分治、递归、哨兵节点 | 递归分治，`merge(lists, left, right)` 对半划分区间递归合并，再用 `mergeTwoLists`（`dummy` 哨兵 + 双指针比较 `list1.val`、`list2.val`）两两归并。 | 第19名 | 112次 | [`java/23.java`](java/23.java) |
| 24 | 两两交换链表中的节点 | swap-nodes-in-pairs | 链表、递归、成对交换 | 两两交换链表相邻节点并返回新头。 | 链表、递归、成对交换、虚拟头节点、迭代、指针交换 | 用 `dummyHead` 哨兵节点，`temp` 每次交换其后两个节点 `node1`、`node2` 的指针指向，然后 `temp` 前移到 `node1`。 | 第91名 | 31次 | [`java/24.java`](java/24.java) |
| 25 | K 个一组翻转链表 | reverse-nodes-in-k-group | 链表、分组翻转、原地操作 | 每 k 个节点一组翻转链表，不足 k 个的尾部保持原顺序。 | 链表、分组翻转、原地操作、链表翻转、k个一组、三指针 | 先遍历求链表长度 `n`，`dummy` 哨兵加 `p0` 记录每组前驱，用 `pre`/`cur`/`next` 三指针逐组翻转 `k` 个节点，翻转后重连前后段，不足 `k` 个保持原序。 | 第3名 | 299次 | [`java/25.java`](java/25.java) |
| 26 | 删除有序数组中的重复项 | remove-duplicates-from-sorted-array | 数组、双指针、原地覆盖 | 原地删除有序数组中的重复元素，使每个元素只出现一次并返回新长度。 | 数组、快慢指针、原地覆盖 | `slow` 指向去重序列尾部，`fast` 向后扫描，`nums[slow]!=nums[fast]` 时执行 `nums[++slow]=nums[fast]`，返回 `slow+1`。 | 第191名 | 11次 | [`java/26.java`](java/26.java) |
| 27 | 移除元素 | remove-element | 数组、双指针、原地覆盖 | 原地移除所有值为 val 的元素并返回新长度。 | 数组、双指针、栈思想、原地覆盖 | 把 `nums` 当作栈，遍历时 `n != val` 才入栈 `nums[stackSize++]=n`，`stackSize` 既是栈大小也是结果长度。 | 未入Top200 | 4次 | [`java/27.java`](java/27.java) |
| 31 | 下一个排列 | next-permutation | 数组、双指针、字典序 | 将数组原地改成字典序下一个排列，不存在则变为最小排列。 | 数组、双指针、字典序、两轮扫描、交换翻转 | 从后往前找第一个 `nums[i] < nums[i+1]` 的 `i`，再从后往前找第一个大于 `nums[i]` 的 `j` 交换，最后 `reverse` 翻转 `[i+1, n)` 使其升序；若 `i<0` 直接整体翻转。 | 第21名 | 103次 | [`java/31.java`](java/31.java) |
| 32 | 最长有效括号 | longest-valid-parentheses | 字符串、栈、动态规划 | 求最长合法括号子串长度。 | 字符串、栈、动态规划、括号匹配 | 栈存括号下标，初始压入 -1 作为最后一个未匹配右括号；遇 `(` 入栈，遇 `)` 出栈，栈空则压入当前下标作新分界，否则用 `i-stack.peek()` 更新 `ans`。 | 第39名 | 69次 | [`java/32.java`](java/32.java) |
| 33 | 搜索旋转排序数组 | search-in-rotated-sorted-array | 数组、二分、旋转有序数组 | 在无重复元素的旋转升序数组中查找目标下标。 | 数组、二分、旋转有序数组、二分查找、旋转数组、有序区间判断 | 二分查找，比较 `nums[left]` 与 `nums[mid]` 判断 `[left,mid]` 是否有序，再判断 `target` 是否落在有序区间内决定去左或右继续。 | 第9名 | 158次 | [`java/33.java`](java/33.java) |
| 34 | 在排序数组中查找元素的第一个和最后一个位置 | find-first-and-last-position-of-element-in-sorted-array | 数组、二分查找 | 在非递减数组中找 target 出现的起始和结束下标，不存在返回 [-1,-1]，要求 O(log n)。 | 数组、二分查找、二分、开区间 lowerBound | `start = lowerBound(target)`，若越界或不等返回 [-1,-1]；`end = lowerBound(target+1)-1`。`lowerBound` 用开区间写法（`left=-1, right=n`，循环不变量 `nums[left]<target<=nums[right]`）找第一个 `>=target` 的下标。 | 第113名 | 24次 | [`java/34.java`](java/34.java) |
| 39 | 组合总和 | combination-sum | 回溯、组合、剪枝 | 从无重复候选数中找所有和为 target 的组合，每个数可重复使用。 | 回溯、组合、剪枝、DFS、搜索树 | DFS 回溯以选/不选构建搜索树，`dfs(idx)` 左支跳过 `candidates[idx]`，右支选入 `combine` 并停留在 `idx`（可重复选），`target==0` 时记录组合，回溯 `remove` 末尾元素。 | 第50名 | 56次 | [`java/39.java`](java/39.java) |
| 41 | 缺失的第一个正数 | first-missing-positive | 数组、原地哈希、置换 | 用 O(n) 时间和 O(1) 额外空间找缺失的最小正整数。 | 数组、原地哈希、置换 | 将值在 `[1,n]` 的元素通过交换放到下标 `nums[i]-1` 处，重复或越界值丢弃；再扫一遍找第一个 `nums[i]!=i+1` 的位置返回。 | 第34名 | 73次 | [`java/41.java`](java/41.java) |
| 42 | 接雨水 | trapping-rain-water | 数组、双指针、单调栈、前后缀最大值 | 给定柱状高度，计算雨后可以接住的水量。 | 数组、双指针、单调栈、前后缀最大值、左右最大值 | 左右双指针向中间靠拢，维护 `leftMax`/`rightMax`，较小一侧的 `max - height` 即该位置接水量，移动较小一侧指针累加。 | 第13名 | 130次 | [`java/42.java`](java/42.java) |
| 43 | 字符串相乘 | multiply-strings | 字符串、模拟、乘法 | 不用大整数库，返回两个非负整数字符串的乘积。 | 字符串、模拟、乘法、模拟乘法、数组、逐位相乘 | 模拟竖式乘法，`res[m+n]` 存结果，`num1[i]*num2[j]` 写入 `res[i+j]`（进位）和 `res[i+j+1]`（低位），最后转字符串跳过前导零。 | 第57名 | 49次 | [`java/43.java`](java/43.java) |
| 45 | 跳跃游戏 II | jump-game-ii | 数组、贪心 | 非负整数数组，最初位于首下标，求到达最后一个下标的最少跳跃次数。 | 数组、贪心、边界计数 | 维护当前步可达边界 `end` 与最远可达 `rightmost`，遍历 `[0,n-1)` 持续更新 `rightmost=max(rightmost, i+nums[i])`，`i==end` 时进入下一步：`end=rightmost`、`steps++`。 | 第129名 | 20次 | [`java/45.java`](java/45.java) |
| 46 | 全排列 | permutations | 回溯、排列、数组 | 返回不含重复数字数组的所有排列。 | 回溯、排列、数组、DFS、全排列 | 回溯 DFS，`path` 记录当前排列，遍历 `nums` 用 `path.contains` 跳过已选元素，递归到 `path.size() == nums.length` 时拷贝 `path` 加入 `ans`，回溯时 `path.remove(size-1)` 移除末尾。 | 第16名 | 123次 | [`java/46.java`](java/46.java) |
| 48 | 旋转图像 | rotate-image | 矩阵、原地旋转、转置 | 将 n x n 矩阵原地顺时针旋转 90 度。 | 矩阵、原地旋转、转置、原地翻转、垂直翻转、对角线翻转 | 顺时针 90 度拆两步原地翻转：先 `flip1` 沿水平中线 `matrix[i][j]` 与 `matrix[n-i-1][j]` 上下翻转，再 `flip2` 沿主对角线转置。 | 第82名 | 37次 | [`java/48.java`](java/48.java) |
| 49 | 字母异位词分组 | group-anagrams | 哈希表、字符串 | 把字母异位词组合在一起，返回分组结果。 | 哈希表、排序、排序作 key、哈希分组 | 每个字符串 `toCharArray` 后 `Arrays.sort`，排序结果作 `HashMap` 的 key，`computeIfAbsent` 分组收集原串。 | 未入Top200 | 9次 | [`java/49.java`](java/49.java) |
| 53 | 最大子数组和 | maximum-subarray | 数组、动态规划、前缀和、Kadane | 求连续子数组的最大和。 | 数组、动态规划、前缀和、Kadane、滚动变量 | 动态规划 `f(i)=max(f(i-1)+nums[i], nums[i])`，用变量 `pre` 滚动记录前一个状态，`maxAns` 维护全局最大值，空间优化到 O(1)。 | 第17名 | 117次 | [`java/53.java`](java/53.java) |
| 54 | 螺旋矩阵 | spiral-matrix | 矩阵、模拟、方向控制 | 按顺时针螺旋顺序返回矩阵所有元素。 | 矩阵、模拟、方向控制、方向数组、访问标记 | 用 `directions` 四方向数组顺时针模拟，`visited` 标记已访问，越界或已访问时 `(directionIdx+1)%4` 换方向。 | 第11名 | 137次 | [`java/54.java`](java/54.java) |
| 55 | 跳跃游戏 | jump-game | 数组、贪心、最远可达 | 判断是否能从数组起点跳到最后一个下标。 | 数组、贪心、最远可达 | 贪心维护能到达的最远位置 `rightmost`，遍历时若 `i <= rightmost` 则更新 `rightmost = max(rightmost, i+nums[i])`，达到 `n-1` 提前返回 true。 | 第95名 | 28次 | [`java/55.java`](java/55.java) |
| 56 | 合并区间 | merge-intervals | 数组、排序、区间合并 | 合并所有重叠区间。 | 数组、排序、区间合并 | 按左端点 `Arrays.sort` 排序，遍历时若 `ans` 末尾区间右端点 `>=` 当前左端点则合并（取右端点最大值），否则加入 `ans`。 | 第33名 | 78次 | [`java/56.java`](java/56.java) |
| 62 | 不同路径 | unique-paths | 动态规划、组合数学、网格路径 | 机器人从左上到右下，只能向右或向下，求路径数。 | 动态规划、组合数学、网格路径、滚动数组、空间优化 | 用 `f[2][n]` 滚动数组，初始 `f[0]` 全为 1，每行先置 `f[curr][0]=1`，再 `f[curr][j]=f[prev][j]+f[curr][j-1]` 表示上方加左方，返回 `f[(m-1)%2][n-1]`，空间优化到 O(n)。 | 第86名 | 35次 | [`java/62.java`](java/62.java) |
| 64 | 最小路径和 | minimum-path-sum | 矩阵、动态规划、记忆化搜索 | 从左上到右下，只能向右或向下，求最小路径和。 | 矩阵、动态规划、记忆化搜索、滚动数组、原地修改 | 用 `dp[2][n]` 滚动数组，`dp[i%2][j]=grid[i][j]+min(dp[(i-1)%2][j], dp[i%2][j-1])`，第一行与每行第一列单独初始化，返回 `dp[(m-1)%2][n-1]`。 | 第72名 | 42次 | [`java/64.java`](java/64.java) |
| 69 | x 的平方根 | sqrtx | 数学、二分、整数运算 | 返回非负整数 x 的算术平方根整数部分。 | 数学、二分、整数运算、数学函数、exp/log | 用 `Math.exp(0.5*Math.log(x))` 计算平方根，再用 `(ans+1)*(ans+1)<=x` 修正取整误差。 | 第40名 | 68次 | [`java/69.java`](java/69.java) |
| 70 | 爬楼梯 | climbing-stairs | 动态规划、斐波那契 | 每次爬 1 或 2 阶，求到第 n 阶的方法数。 | 动态规划、斐波那契、滚动变量 | `f(n)=f(n-1)+f(n-2)`，`n<=2` 直接返回 n，否则用滚动变量 `prev2`、`prev1` 迭代更新，空间 O(1)。 | 第81名 | 38次 | [`java/70.java`](java/70.java) |
| 72 | 编辑距离 | edit-distance | 字符串、动态规划、编辑距离 | 计算将一个单词转换为另一个单词的最少插入、删除、替换次数。 | 字符串、动态规划、编辑距离、滚动数组 | `D[2][m+1]` 滚动数组表示 `dp[i][j]`（word1 前 i 个字符到 word2 前 j 个字符的编辑距离），由插入 `D[curr][j-1]+1`、删除 `D[prev][j]+1`、替换 `D[prev][j-1]`（字符不同时再 +1）三者取最小，用 `i%2`、`(i-1)%2` 切换当前行与上一行。 | 第28名 | 85次 | [`java/72.java`](java/72.java) |
| 73 | 矩阵置零 | set-matrix-zeroes | 矩阵、原地标记 | 若矩阵中某元素为 0，则将其所在行和列全部置 0，要求原地操作。 | 矩阵、原地标记、原数组首行首列做标记、O(1)空间 | 先用两个布尔变量记录第一行/列本身是否有 0；再扫描内部矩阵，遇 0 把 `matrix[0][j]`、`matrix[i][0]` 置 0 作标记；第二遍按标记将内部格子置 0；最后按两个布尔变量处理第一行/列。 | 未入Top200 | 2次 | [`java/73.java`](java/73.java) |
| 74 | 搜索二维矩阵 | search-a-2d-matrix | 矩阵、二分查找 | 每行升序且下一行首元素大于上一行末元素的矩阵中查找 target。 | 矩阵、二分查找、两次二分 | 先对第一列二分（`searchFirstCol` 返回 `high` 即 `<=target` 的最后一行行标），再对该行 `Arrays.binarySearch` 判定。 | 第127名 | 20次 | [`java/74.java`](java/74.java) |
| 76 | 最小覆盖子串 | minimum-window-substring | 字符串、滑动窗口、哈希计数 | 在 s 中找包含 t 全部字符的最短子串。 | 字符串、滑动窗口、哈希计数、字符计数 | 滑动窗口配 `cntS[128]`、`cntT[128]` 计数，右指针扩张累加字符，`isCover` 判断窗口涵盖 `t` 后左指针收缩求最短窗口，记录 `ansLeft`、`ansRight`。 | 第52名 | 52次 | [`java/76.java`](java/76.java) |
| 78 | 子集 | subsets | 回溯、位运算、子集枚举 | 返回不含重复元素数组的所有子集。 | 回溯、位运算、子集枚举、DFS、选或不选 | DFS 对每个元素讨论选或不选，到 `i==nums.length` 时将 `path` 复制加入 `ans`，选后 `path.remove` 回溯。 | 第60名 | 49次 | [`java/78.java`](java/78.java) |
| 79 | 单词搜索 | word-search | 回溯、矩阵、DFS | 判断单词是否能由网格中相邻字符路径组成，每格最多用一次。 | 回溯、矩阵、DFS、原地标记 | 遍历网格找到首字符后 DFS 四方向扩展，用 `board[r][c]='#'` 原地标记访问，回溯时用 `temp` 恢复，`index` 到 `word.length()` 即找到。 | 第76名 | 41次 | [`java/79.java`](java/79.java) |
| 80 | 删除有序数组中的重复项 II | remove-duplicates-from-sorted-array-ii | 数组、双指针 | 原地删除有序数组中超过两次的重复项，使每个元素最多出现两次并返回新长度。 | 数组、双指针、栈思想、入栈条件判重 | 类似 LC27：`stackSize` 从 2 起步（前两个直接保留），当 `nums[i] != nums[stackSize-2]` 时才入栈 `nums[stackSize++]=nums[i]`。 | 未入Top200 | 7次 | [`java/80.java`](java/80.java) |
| 82 | 删除排序链表中的重复元素 II | remove-duplicates-from-sorted-list-ii | 链表、哑节点、去重 | 删除有序链表中所有重复值节点，只保留未重复的值。 | 链表、哑节点、去重、链表去重、dummy 哨兵、连续删除 | `dummy` 哨兵，`cur` 扫描，发现 `cur.next.val==cur.next.next.val` 时记下 `x` 并连续删除所有值为 `x` 的节点。 | 第56名 | 49次 | [`java/82.java`](java/82.java) |
| 83 | 删除排序链表中的重复元素 | remove-duplicates-from-sorted-list | 链表、一次遍历 | 删除排序链表中重复的元素使每个元素只出现一次。 | 链表、一次遍历、相邻比较、跳过重复 | 重复元素在有序链表中连续，`cur.val==cur.next.val` 时 `cur.next=cur.next.next` 删除，否则 `cur` 后移。 | 第122名 | 22次 | [`java/83.java`](java/83.java) |
| 88 | 合并两个有序数组 | merge-sorted-array | 数组、双指针、原地合并 | 将两个非降序数组合并到第一个数组中。 | 数组、双指针、原地合并、逆向双指针、从后往前填 | 逆向双指针 `p1=m-1`、`p2=n-1`、`p=m+n-1` 从各数组末尾出发，每次取较大者填入 `nums1[p--]`，循环条件 `p2 >= 0` 直到 `nums2` 合并完。 | 第22名 | 94次 | [`java/88.java`](java/88.java) |
| 92 | 反转链表 II | reverse-linked-list-ii | 链表、区间翻转、指针操作 | 只反转链表中从 left 到 right 的一段。 | 链表、区间翻转、指针操作、链表翻转、dummy 节点、三指针 | 加 `dummy` 头节点，先定位翻转段前驱 `p0`，再用 `pre`、`cur`、`next` 三指针迭代翻转 `[left,right]` 段，最后 `p0.next.next=cur`、`p0.next=pre` 完成重连。 | 第31名 | 80次 | [`java/92.java`](java/92.java) |
| 93 | 复原IP地址 | restore-ip-addresses | 字符串、回溯、分段枚举 | 从字符串中恢复所有合法 IPv4 地址。 | 字符串、回溯、分段枚举、DFS、字符串分割 | DFS 回溯，`dfs(start, path)` 从 `start` 起尝试取 1-3 个字符作为段，用 `validIpAdddr` 校验无前导零且值在 0-255，凑齐 4 段且用完字符则加入 `ans`，回溯移除末尾段。 | 第45名 | 62次 | [`java/93.java`](java/93.java) |
| 94 | 二叉树的中序遍历 | binary-tree-inorder-traversal | 二叉树、DFS、中序遍历 | 返回二叉树的中序遍历结果。 | 二叉树、DFS、中序遍历、递归 | 递归实现中序遍历，`doInorder` 先遍历左子树，访问 `root.val` 加入 `ans`，再遍历右子树。 | 第69名 | 45次 | [`java/94.java`](java/94.java) |
| 98 | 验证二叉搜索树 | validate-binary-search-tree | 二叉树、BST、递归、中序遍历 | 判断二叉树是否为合法二叉搜索树。 | 二叉树、BST、递归、中序遍历、区间约束、BST 验证 | 递归传递 `(lower,upper)` 开区间约束，`node.val<=lower` 或 `node.val>=upper` 即非法，左子树上界收紧为 `node.val`，右子树下界收紧为 `node.val`。 | 第66名 | 46次 | [`java/98.java`](java/98.java) |
| 101 | 对称二叉树 | symmetric-tree | 二叉树、递归、镜像 | 判断二叉树是否关于中心轴对称。 | 二叉树、递归、镜像、双指针、镜像判断 | 递归双指针 `check(p, q)` 让 `p`、`q` 朝反方向移动，比较根值后递归判断 `p` 的左子树与 `q` 的右子树、`p` 的右子树与 `q` 的左子树是否互为镜像。 | 第43名 | 63次 | [`java/101.java`](java/101.java) |
| 102 | 二叉树的层序遍历 | binary-tree-level-order-traversal | 二叉树、BFS、队列 | 按层返回二叉树节点值。 | 二叉树、BFS、队列、层序遍历 | BFS 用 `queue`，每轮 `currentLevelSize = queue.size()` 确定当前层节点数，逐个 `poll` 收集到 `level` 并把左右孩子 `offer` 入队，`ans.add(level)`。 | 第26名 | 88次 | [`java/102.java`](java/102.java) |
| 103 | 二叉树的锯齿形层次遍历 | binary-tree-zigzag-level-order-traversal | 二叉树、BFS、层序遍历、双端队列 | 按层遍历二叉树，层与层之间左右方向交替输出。 | 二叉树、BFS、层序遍历、双端队列 | `Deque` 队列做层序 BFS，用 `leftToRight` 标志当前层方向，奇数层 `addLast`、偶数层 `addFirst`，每层结束翻转方向。 | 第7名 | 186次 | [`java/103.java`](java/103.java) |
| 104 | 二叉树的最大深度 | maximum-depth-of-binary-tree | 二叉树、DFS | 求二叉树的最大深度（根节点到最远叶子节点的最长路径上的节点数）。 | 二叉树、DFS、递归、后序求高 | `maxDepth(root) = max(maxDepth(left), maxDepth(right)) + 1`，空节点返回 0。 | 第101名 | 28次 | [`java/104.java`](java/104.java) |
| 105 | 从前序与中序遍历序列构造二叉树 | construct-binary-tree-from-preorder-and-inorder-traversal | 二叉树、递归、哈希表 | 根据前序和中序遍历结果构造二叉树。 | 二叉树、递归、哈希表、前序中序 | 用 `indexMap` 记录中序值下标，前序首位为根，定位中序根位置 `pos` 后算出左子树节点数 `leftNum`，递归构建左右子树并更新前序中序索引范围。 | 第44名 | 62次 | [`java/105.java`](java/105.java) |
| 108 | 将有序数组转换为二叉搜索树 | convert-sorted-array-to-binary-search-tree | 二叉树、BST、分治 | 将升序数组转换为一棵高度平衡的二叉搜索树。 | 二叉树、分治、取中点为根 | `buildTree(left,right)` 取 `mid = left+(right-left)/2`（向下取整）的元素为根，递归构建左右子区间。 | 未入Top200 | 7次 | [`java/108.java`](java/108.java) |
| 110 | 平衡二叉树 | balanced-binary-tree | 二叉树、DFS、树高 | 判断二叉树任意节点左右子树高度差是否不超过 1。 | 二叉树、DFS、树高、后序遍历、提前剪枝 | DFS 后序遍历求高度，不平衡时返回 -1 提前剪枝，左右子树高度差超过 1 即标记 -1，最终判断 `height(root) >= 0`。 | 第100名 | 28次 | [`java/110.java`](java/110.java) |
| 112 | 路径总和 | path-sum | 二叉树、DFS、路径和 | 判断树中是否存在根到叶路径，其节点和等于 target。 | 二叉树、DFS、路径和、递归、叶子判断 | 递归自顶向下累减 `targetSum`，到叶子节点判断 `targetSum==root.val`，左右子树任一满足即返回 true。 | 第64名 | 47次 | [`java/112.java`](java/112.java) |
| 113 | 路径总和 II | path-sum-ii | 二叉树、DFS、回溯、路径和 | 返回所有根到叶路径和等于 target 的路径。 | 二叉树、DFS、回溯、路径和、路径记录 | 前序 DFS 用 `path` 记录路径并扣减 `targetSum`，到叶子且 `targetSum==0` 时把 `path` 拷贝加入 `ans`，递归返回时 `path.remove(path.size()-1)` 回溯。 | 第73名 | 42次 | [`java/113.java`](java/113.java) |
| 114 | 二叉树展开为链表 | flatten-binary-tree-to-linked-list | 二叉树、前序遍历 | 将二叉树原地展开为链表：展开后的单链表与前序遍历一致，均用 right 指针指向。 | 二叉树、前序遍历、线性表重串 | 先 `preOrder` 把节点存入 `list`，再遍历前 `n-1` 个节点，`cur.left=null`、`cur.right=list.get(i+1)` 依次串接。 | 第102名 | 26次 | [`java/114.java`](java/114.java) |
| 118 | 杨辉三角 | pascals-triangle | 数组、模拟 | 生成杨辉三角的前 numRows 行。 | 数组、模拟、逐行构造 | 每行首尾补 1，中间元素 `a[i][j]=a[i-1][j-1]+a[i-1][j]`，由上一行推出当前行。 | 未入Top200 | 1次 | [`java/118.java`](java/118.java) |
| 121 | 买卖股票的最佳时机 | best-time-to-buy-and-sell-stock | 数组、贪心、一次遍历 | 只能买卖一次股票，求可获得的最大利润。 | 数组、贪心、一次遍历、最小买入价 | 一次遍历，维护历史最低价 `minPrice`，每步用 `price-minPrice` 更新最大利润 `ans`，同时更新 `minPrice`。 | 第10名 | 148次 | [`java/121.java`](java/121.java) |
| 122 | 买卖股票的最佳时机 II | best-time-to-buy-and-sell-stock-ii | 数组、贪心、动态规划 | 可多次买卖股票，求最大利润。 | 数组、贪心、累加正向差价 | 贪心：只计正贡献区间，遍历累加相邻正向差价 `ans += Math.max(0, prices[i]-prices[i-1])`，即为最大利润。 | 第62名 | 48次 | [`java/122.java`](java/122.java) |
| 124 | 二叉树中的最大路径和 | binary-tree-maximum-path-sum | 二叉树、DFS、树形 DP | 求二叉树中任意非空路径的最大路径和。 | 二叉树、DFS、树形 DP、后序 DFS、最大路径和 | 后序 `maxGain` 返回以当前节点为端点的单侧最大贡献（负数取 0），在转弯处用 `leftGain+rightGain+root.val` 更新全局 `maxSum`。 | 第32名 | 78次 | [`java/124.java`](java/124.java) |
| 125 | 验证回文串 | valid-palindrome | 字符串、双指针 | 判断字符串去掉非字母数字字符并忽略大小写后是否为回文。 | 字符串、双指针、跳过非字母数字 | `i`、`j` 双指针向中间靠拢，非字母数字则跳过，否则统一 `toLowerCase` 比较，不等返回 false。 | 第165名 | 14次 | [`java/125.java`](java/125.java) |
| 128 | 最长连续序列 | longest-consecutive-sequence | 哈希表、数组、连续序列 | 在未排序数组中求最长连续整数序列长度。 | 哈希表、数组、连续序列、哈希集合、序列起点、向上计数 | 数存入 `HashSet`，仅对 `num-1` 不在集合中的起点向上 `while set.contains(currentNum+1)` 计数，`ans` 取最大长度。 | 第78名 | 40次 | [`java/128.java`](java/128.java) |
| 129 | 求根到叶子节点数字之和 | sum-root-to-leaf-numbers | 二叉树、DFS、路径数字 | 每条根到叶路径形成一个数字，求所有数字之和。 | 二叉树、DFS、路径数字、路径求和 | DFS 传递 `pathSum`，访问节点时 `pathSum = pathSum * 10 + root.val`，到达叶子节点时累加到 `ans`，递归遍历左右子树。 | 第48名 | 59次 | [`java/129.java`](java/129.java) |
| 134 | 加油站 | gas-station | 贪心、数组 | 环形路线上 n 个加油站，gas[i] 加油、cost[i] 耗油，判断从哪个站出发能环行一圈。 | 贪心、折线图最低点、一次遍历 | 累计油量 `s` 走完全程：`s<0` 必无解；否则从折线图最低点的下一站 `ans` 出发一定可行——维护 `minS` 与 `ans`，`s<minS` 时更新并令 `ans=i+1`。 | 未入Top200 | 10次 | [`java/134.java`](java/134.java) |
| 135 | 分发糖果 | candy | 贪心、两次扫描、数组 | 按评分给孩子分糖，评分更高的相邻孩子糖更多，求最少糖数。 | 贪心、两次扫描、数组、常数空间、递增递减序列 | 一次遍历，用 `inc`、`dec`、`pre` 跟踪递增/递减序列长度，评分相等时 `pre` 置 1，递减序列长度追上递增时 `dec++` 补偿，累加糖果数 `ret`。 | 第96名 | 28次 | [`java/135.java`](java/135.java) |
| 136 | 只出现一次的数字 | single-number | 位运算、哈希表 | 非空数组中除一个元素只出现一次外其余均出现两次，找出那个元素。 | 数组、哈希、哈希计数 | `mapCount`（HashMap）统计每个数出现次数，再遍历 `entrySet` 返回次数为 1 的 key。（亦可用全体异或 O(1) 空间解决） | 未入Top200 | 9次 | [`java/136.java`](java/136.java) |
| 138 | 复制带随机指针的链表 | copy-list-with-random-pointer | 链表、哈希表 | 深拷贝一个长度为 n 的链表，每个节点额外有 random 指针。 | 链表、哈希、顺序表下标映射 | 两个 `ArrayList` 分别保存原链表与新链表节点，先按 next 拷贝 val 建新链表，再用 `list.indexOf(cur.random)` 找到 random 对应的新节点 `list2.get(idx)` 建立关联。 | 第108名 | 26次 | [`java/138.java`](java/138.java) |
| 139 | 单词拆分 | word-break | 字符串、动态规划、Trie | 判断字符串是否能由字典中的单词拼接而成。 | 字符串、动态规划、Trie、哈希集合、字符串拆分 | `dp[i]` 表示 `s[0..i-1]` 能否拆分，字典存入 `HashSet`；枚举分割点 j，`dp[i]=dp[j] && wordDictSet.contains(s.substring(j,i))`，命中即 break。 | 第75名 | 42次 | [`java/139.java`](java/139.java) |
| 141 | 环形链表 | linked-list-cycle | 链表、快慢指针、环检测 | 判断链表中是否存在环。 | 链表、快慢指针、环检测、环形链表 | `slow` 走一步、`fast` 走两步，若相遇则有环；`fast` 或 `fast.next` 为 null 则无环。 | 第30名 | 82次 | [`java/141.java`](java/141.java) |
| 142 | 环形链表 II | linked-list-cycle-ii | 链表、快慢指针、环入口 | 返回链表入环的第一个节点，无环返回 null。 | 链表、快慢指针、环入口、Floyd判圈 | Floyd 判圈，`slow`、`fast` 快慢指针相遇确认有环，再让 `slow` 与 `head` 同步前进，相遇点即环入口节点。 | 第58名 | 49次 | [`java/142.java`](java/142.java) |
| 143 | 重排链表 | reorder-list | 链表、快慢指针、反转、合并 | 将链表重排为首尾交替的顺序。 | 链表、快慢指针、反转、合并、链表逆序、交替合并 | 快慢指针 `middleNode` 找中点断开，`reverseList`（`prev`、`curr` 迭代）反转后半段 `l2`，再 `mergeList` 把 `l1`、`l2` 交替拼接。 | 第23名 | 91次 | [`java/143.java`](java/143.java) |
| 144 | 二叉树的前序遍历 | binary-tree-preorder-traversal | 二叉树、DFS | 返回二叉树的前序遍历结果。 | 二叉树、DFS、递归、根左右 | 递归实现：访问 `root.val` 加入 `ans`，再依次递归左、右子树。 | 第154名 | 16次 | [`java/144.java`](java/144.java) |
| 146 | LRU缓存机制 | lru-cache | 设计、哈希表、双向链表、LRU | 实现支持 O(1) `get` 和 `put` 的最近最少使用缓存。 | 设计、哈希表、双向链表、LRU | `cache` 哈希表定位节点，`dummyhead`/`dummytail` 伪首尾双向链表维护最近使用顺序，访问移到头部，超容量删尾部。 | 第2名 | 362次 | [`java/146.java`](java/146.java) |
| 148 | 排序链表 | sort-list | 链表、排序、归并 | 在 O(n log n) 时间内对链表排序。 | 链表、排序、归并、快慢指针找中点、递归 | 递归归并排序：快慢指针找中点断开链表，`sortList` 分别递归排序左右两段，再 `merge`（`dummy` 哨兵 + 双指针比较）合并两个有序链表。 | 第38名 | 69次 | [`java/148.java`](java/148.java) |
| 151 | 翻转字符串里的单词 | reverse-words-in-a-string | 字符串、双指针、分词 | 去掉多余空格并反转字符串中单词顺序。 | 字符串、双指针、分词、字符串分割、逆序拼接、trim | `trim()` 去首尾空格后按 `\\s+` 分割成 `ar`，逆序遍历用 `StringBuilder` 拼接单词。 | 第98名 | 28次 | [`java/151.java`](java/151.java) |
| 152 | 乘积最大子数组 | maximum-product-subarray | 数组、动态规划、最大最小状态 | 求连续子数组的最大乘积。 | 数组、动态规划、最大最小状态、滚动变量、最大最小乘积 | 维护滚动变量 `fMax`、`fMin`，每步从 `fMaxOld*nums[i]`、`fMin*nums[i]`、`nums[i]` 三者取最大最小更新（处理负数翻转），`ans` 记录全局最大。 | 第80名 | 39次 | [`java/152.java`](java/152.java) |
| 153 | 寻找旋转排序数组中的最小值 | find-minimum-in-rotated-sorted-array | 数组、二分查找 | 无重复元素的旋转升序数组中找最小值。 | 数组、二分、与右端点比较 | `nums[mid] > nums[right]` 说明最小值在 mid 右段（`left=mid+1`），否则在含 mid 的左段（`right=mid`），区间收敛到 `left==right` 即最小值。 | 第144名 | 17次 | [`java/153.java`](java/153.java) |
| 155 | 最小栈 | min-stack | 栈、设计、辅助栈 | 设计支持常数时间获取最小值的栈。 | 栈、设计、辅助栈、同步最小值 | 双栈 `xStack` 存数据、`minStack` 同步存当前最小值，入栈时 `minStack.push(Math.min(minStack.peek(), x))`，`getMin` 直接取 `minStack.peek()`。 | 第74名 | 42次 | [`java/155.java`](java/155.java) |
| 160 | 相交链表 | intersection-of-two-linked-lists | 链表、双指针、指针切换 | 返回两个单链表开始相交的节点，不相交返回 null。 | 链表、双指针、指针切换、链表拼接、O(1)空间 | 双指针 `pA`、`pB` 分别从 `headA`、`headB` 出发，走到末尾（`null`）后切换到另一链表头继续，两指针走过的总长度相同，最终在相交点相遇或同时到达 `null`。 | 第18名 | 115次 | [`java/160.java`](java/160.java) |
| 162 | 寻找峰值 | find-peak-element | 数组、二分、峰值 | 找到任意一个严格大于相邻元素的峰值下标。 | 数组、二分、峰值、二分查找、峰值元素、对相邻比较 | 二分比较 `nums[mid]` 与 `nums[mid+1]`，下降则峰值在左缩 `right=mid`，上升则在右缩 `left=mid+1`，区间收敛到峰值下标。 | 第59名 | 49次 | [`java/162.java`](java/162.java) |
| 165 | 比较版本号 | compare-version-numbers | 字符串、双指针、模拟 | 比较两个版本号字符串的大小。 | 字符串、双指针、模拟、逐段解析数字、版本号比较 | 双指针 `i`、`j` 各自逐字符解析出一段数字 `x`、`y`（遇 `.` 停止并跳过点号，不存在的段视为 0），不等即返回 1 或 -1，全部相等返回 0。 | 第35名 | 71次 | [`java/165.java`](java/165.java) |
| 167 | 两数之和 II - 输入有序数组 | two-sum-ii-input-array-is-sorted | 数组、双指针、二分查找 | 在升序数组中找和为 target 的两个数，返回下标（1 起）。 | 数组、相向双指针 | `left`、`right` 相向而行：和大于 target 则 `right--`，小于则 `left++`，相等时返回 `[left+1, right+1]`。 | 未入Top200 | 6次 | [`java/167.java`](java/167.java) |
| 169 | 多数元素 | majority-element | 数组、投票算法 | 求出现次数大于 n/2 的多数元素。 | 数组、投票算法、Boyer-Moore 投票 | `count==0` 时把当前元素设为 `candidate`，与候选相同 `count+1` 否则 `-1`，抵消之后多数元素仍占多数，最终 `candidate` 即答案。 | 第126名 | 21次 | [`java/169.java`](java/169.java) |
| 179 | 最大数 | largest-number | 排序、贪心、自定义比较 | 将非负整数数组排列成能组成的最大数字字符串。 | 排序、贪心、自定义比较、自定义排序、字符串拼接、降序 | 将数字转为字符串数组 `numStrs`，按 `(a,b)->(b+a).compareTo(a+b)` 降序排序后拼接，特判首元素为 "0" 时返回 "0"。 | 第90名 | 31次 | [`java/179.java`](java/179.java) |
| 189 | 轮转数组 | rotate-array | 数组、翻转 | 将数组元素向右轮转 k 个位置。 | 数组、翻转、三次反转 | `k%=n` 后三次原地反转：整体翻转 `[0,n-1]`，再分别翻转 `[0,k-1]` 与 `[k,n-1]`。 | 第172名 | 13次 | [`java/189.java`](java/189.java) |
| 198 | 打家劫舍 | house-robber | 动态规划、滚动数组 | 相邻房屋不能同时偷，求可偷的最大金额。 | 动态规划、滚动数组、滚动变量、打家劫舍 | `dp[i]=max(dp[i-2]+nums[i], dp[i-1])`，用滚动变量 `prev2`、`prev1` 迭代更新，空间 O(1)。 | 第85名 | 36次 | [`java/198.java`](java/198.java) |
| 199 | 二叉树的右视图 | binary-tree-right-side-view | 二叉树、DFS、BFS、层序 | 返回从右侧能看到的二叉树节点值。 | 二叉树、DFS、BFS、层序、右子树优先、深度首次访问 | DFS 先递归 `root.right` 再递归 `root.left`，当 `depth == ans.size()` 表示该深度首次访问即右视图节点，加入 `ans`。 | 第25名 | 90次 | [`java/199.java`](java/199.java) |
| 200 | 岛屿数量 | number-of-islands | 网格、DFS、BFS、连通分量 | 给定由陆地和水组成的网格，统计四方向相连的岛屿数量。 | 网格、DFS、BFS、连通分量、岛屿计数、原标记修改 | 遍历网格，遇到 `'1'` 启动 DFS 并计数，DFS 中将访问过的陆地标记为 `'2'`，向四个方向递归直到越界或非陆地。 | 第6名 | 189次 | [`java/200.java`](java/200.java) |
| 206 | 反转链表 | reverse-linked-list | 链表、指针反转、迭代 | 反转单链表并返回新的头节点。 | 链表、指针反转、迭代、链表翻转、三指针 | 用 `pre`/`cur`/`next` 三指针迭代翻转，`cur.next` 指向 `pre` 后逐节点后移，最后 `dummy.next` 指向翻转后的头节点。 | 第4名 | 241次 | [`java/206.java`](java/206.java) |
| 207 | 课程表 | course-schedule | 图、DFS、拓扑排序、环检测 | 判断是否可以完成所有课程，即先修依赖图是否无环。 | 图、DFS、拓扑排序、环检测、三色标记法、邻接表 | 构建邻接表 `graph[]`，用三色标记 `colors[]`（0 未访问/1 访问中/2 已访问）做 DFS，遇到访问中节点说明有环则返回 false。 | 第89名 | 34次 | [`java/207.java`](java/207.java) |
| 209 | 长度最小的子数组 | minimum-size-subarray-sum | 数组、滑动窗口、前缀和 | 找和至少为 target 的最短连续子数组长度。 | 数组、滑动窗口、前缀和、求和 | 滑动窗口，`right` 扩张累加 `sum`，当 `sum >= target` 时收缩 `left` 并更新最短长度 `ans`，找不到返回 0。 | 第54名 | 51次 | [`java/209.java`](java/209.java) |
| 215 | 数组中的第K个最大元素 | kth-largest-element-in-an-array | 数组、快速选择、堆、排序 | 在未排序数组中返回第 k 大元素，而不是第 k 个不同元素。 | 数组、快速选择、堆、排序、随机pivot、三路划分 | 将第 k 大转成升序下标 `target = n-k`，随机选 `pivot` 后用 `lt`/`i`/`gt` 三路划分，判断目标索引所在区间只继续该侧。 | 第5名 | 205次 | [`java/215.java`](java/215.java) |
| 221 | 最大正方形 | maximal-square | 矩阵、动态规划 | 在 0/1 矩阵中找只包含 1 的最大正方形面积。 | 矩阵、动态规划、滚动数组、最大正方形 | `dp[curr][j]` 表示以 `(i,j)` 为右下角的全 1 正方形最大边长（滚动数组 `dp[2][cols]`，`curr`/`prev` 交替），由左、左上、上三方最小值加 1 转移，边界为 1，非 '1' 时需清零，答案取最大边长的平方 `ans*ans`。 | 第36名 | 71次 | [`java/221.java`](java/221.java) |
| 224 | 基本计算器 | basic-calculator | 栈、递归、表达式求值 | 计算包含加减和括号的表达式字符串。 | 栈、递归、表达式求值、符号作用域、括号处理 | 用栈 `ops` 记录括号作用域符号，遇 `(` 入栈当前 `sign`，遇 `)` 出栈，遇 `+`/`-` 取栈顶设置 `sign`，数字按 `sign*num` 累加到 `ret`。 | 第99名 | 28次 | [`java/224.java`](java/224.java) |
| 226 | 翻转二叉树 | invert-binary-tree | 二叉树、递归 | 翻转二叉树（镜像每个节点的左右子树）。 | 二叉树、递归、递归交换 | 先递归求出翻转后的左右子树根 `newRight`/`newLeft`（避免提前改指针丢失子树），再赋给 `root.right`/`root.left`。 | 第104名 | 26次 | [`java/226.java`](java/226.java) |
| 230 | 二叉搜索树中第K小的元素 | kth-smallest-element-in-a-bst | 二叉树、BST、中序遍历 | 求 BST 中第 k 小的元素。 | BST、中序遍历、中序遍历即升序 | 中序递归（左-根-右）把节点值按升序收集进 `ans`，返回 `ans.get(k-1)`。 | 第120名 | 23次 | [`java/230.java`](java/230.java) |
| 232 | 用栈实现队列 | implement-queue-using-stacks | 栈、队列、设计 | 只用栈实现队列的 push、pop、peek、empty。 | 栈、队列、设计、双栈、栈翻转 | 双栈 `inStack`、`outStack`，`push` 入 `inStack`，`pop`/`peek` 时若 `outStack` 空则调用 `in2out` 把输入栈倒入输出栈再取栈顶。 | 第53名 | 52次 | [`java/232.java`](java/232.java) |
| 234 | 回文链表 | palindrome-linked-list | 链表、快慢指针 | 判断链表是否为回文，要求 O(n) 时间 O(1) 空间。 | 链表、快慢指针、找中点、反转后半、比较后恢复 | 先计数 n 定位前半段末节点 `last`（前半段更长），`reverse` 反转后半段后双指针逐一比较，最后再反转恢复链表原状。 | 第110名 | 25次 | [`java/234.java`](java/234.java) |
| 236 | 二叉树的最近公共祖先 | lowest-common-ancestor-of-a-binary-tree | 二叉树、递归、最近公共祖先 | 在二叉树中找到两个指定节点的最近公共祖先。 | 二叉树、递归、最近公共祖先、DFS、后序遍历、公共祖先 | 后序 DFS 递归查找，`root==p` 或 `root==q` 直接返回，左右子树都非空则 `root` 为最近公共祖先，否则返回非空一侧。 | 第14名 | 130次 | [`java/236.java`](java/236.java) |
| 238 | 除自身以外数组的乘积 | product-of-array-except-self | 数组、前缀乘积 | 不用除法，返回每个位置其余所有元素的乘积。 | 数组、前缀乘积、前缀乘积 + 后缀变量 | `ans[i]` 先存左侧乘积 `L[i]`（`ans[i]=ans[i-1]*nums[i-1]`），再从右往左用变量 `R` 累乘右侧乘积：`ans[i]*=R; R*=nums[i]`，空间 O(1)。 | 未入Top200 | 6次 | [`java/238.java`](java/238.java) |
| 239 | 滑动窗口最大值 | sliding-window-maximum | 队列、单调队列、滑动窗口 | 返回每个长度为 k 的滑动窗口中的最大值。 | 队列、单调队列、滑动窗口、双端队列、滑动窗口最大值 | 双端队列 `deque` 存下标维护单调递减，入窗弹出队尾 `<=nums[i]` 的下标，队首出窗即移除，`i>=k-1` 时记录 `nums[deque.getFirst()]`。 | 第63名 | 47次 | [`java/239.java`](java/239.java) |
| 240 | 搜索二维矩阵 II | search-a-2d-matrix-ii | 矩阵、二分、搜索剪枝 | 在每行每列均升序的矩阵中查找 target。 | 矩阵、二分、搜索剪枝、右上角搜索、类 BST、缩小范围 | 从右上角 `(0,n-1)` 出发，`matrix[x][y]>target` 向左走，否则向下走，类似 BST 搜索直到越界或命中。 | 第71名 | 43次 | [`java/240.java`](java/240.java) |
| 274 | H 指数 | h-index | 数组、计数排序 | 求研究者的 h 指数：h 篇论文每篇至少被引 h 次。 | 数组、计数排序、从大到小找 | `count` 计数（引用数 `>=n` 一律记入 `count[n]`），从 `i=n` 往小累计 `ans+=count[i]`，首个满足 `ans>=i` 的 `i` 即 h 指数。 | 未入Top200 | 1次 | [`java/274.java`](java/274.java) |
| 283 | 移动零 | move-zeroes | 数组、双指针 | 原地把所有 0 移到末尾，保持非零元素相对顺序。 | 数组、双指针、双指针交换 | `left` 指向已处理序列尾部，`right` 扫描找非零元素，找到即与 `left` 交换并 `left++`。 | 第121名 | 22次 | [`java/283.java`](java/283.java) |
| 287 | 寻找重复数 | find-the-duplicate-number | 数组、快慢指针 | n+1 个数都在 [1,n] 内，找重复的那个数（不修改数组）。 | 数组、哈希计数、HashMap | `mapCount`（HashMap）统计每个数出现次数，遍历 entrySet 返回次数大于 1 的 key。（标准解法：把值当 next 指针用 Floyd 快慢指针判圈，O(1) 空间） | 第142名 | 17次 | [`java/287.java`](java/287.java) |
| 300 | 最长上升子序列 | longest-increasing-subsequence | 数组、动态规划、二分、贪心 | 求数组中最长严格递增子序列的长度。 | 数组、动态规划、二分、贪心、二分查找、原地修改、开区间写法 | 贪心+二分，复用 `nums` 作 `tails` 数组，对每个 `x` 用开区间二分 `lowerBound`（`left=-1, right=len`）找第一个 `>=x` 的位置替换，`res` 记录 LIS 长度。 | 第20名 | 107次 | [`java/300.java`](java/300.java) |
| 303 | 区域和检索 - 数组不可变 | range-sum-query-immutable | 设计、前缀和 | 求数组区间 [left,right] 的和，多次查询。 | 设计、前缀和 | 构造时预处理前缀和 `s[i+1]=s[i]+nums[i-1]`，查询 `sumRange(l,r)=s[r+1]-s[l]`。 | 未入Top200 | 3次 | [`java/303.java`](java/303.java) |
| 322 | 零钱兑换 | coin-change | 动态规划、完全背包、记忆化搜索 | 用给定硬币凑出指定金额，求最少硬币数。 | 动态规划、完全背包、记忆化搜索、DP、滚动数组 | 完全背包 DP 用滚动数组 `f[2][amount+1]`，`f[(i+1)%2][c]` 取不选 `f[i%2][c]` 与选 `f[(i+1)%2][c-coins[i]]+1` 的最小值，初值 `Integer.MAX_VALUE/2` 防溢出，无解返回 -1。 | 第47名 | 59次 | [`java/322.java`](java/322.java) |
| 329 | 矩阵中的最长递增路径 | longest-increasing-path-in-a-matrix | 矩阵、DFS、记忆化搜索、拓扑排序 | 求矩阵中四方向移动形成的最长严格递增路径长度。 | 矩阵、DFS、记忆化搜索、拓扑排序、备忘录、四方向探索 | 对每个格子做 DFS 探索 `dirs` 四方向严格递增路径，`memo[][]` 记录已算结果避免重复计算，取全局最大值 `maxLength`。 | 第93名 | 29次 | [`java/329.java`](java/329.java) |
| 380 | O(1) 时间插入、删除和获取随机元素 | insert-delete-getrandom-o1 | 设计、哈希表 | 设计平均 O(1) 完成插入、删除和等概率随机获取的数据结构。 | 设计、哈希+动态数组、哈希表 + 动态数组、尾部补位删除 | `map` 存「值->下标」配合 `list` 存值；插入追加到尾部并记下标；删除时把尾元素 `last` 拷到被删下标处并更新其 map 映射，再删尾部，保证 O(1)；随机用 `random.nextInt(list.size())`。 | 第159名 | 15次 | [`java/380.java`](java/380.java) |
| 392 | 判断子序列 | is-subsequence | 字符串、双指针、子序列 | 判断 s 是否为 t 的子序列。 | 字符串、双指针、双指针匹配 | 指针 `i` 扫描 s：遍历 t 的每个字符，与 `s.charAt(i)` 相等则 `i++`，`i==s.length()` 即为子序列；空串视为任何串的子序列。 | 未入Top200 | 10次 | [`java/392.java`](java/392.java) |
| 394 | 字符串解码 | decode-string | 栈、字符串、递归解析 | 解码形如 `k[encoded_string]` 的嵌套字符串。 | 栈、字符串、递归解析、递归、字符串解析、指针 | 递归解析，全局指针 `ptr` 扫描字符串，遇数字用 `getDigits` 取重复次数 `repTime`，递归解析括号内子串后重复拼接，遇字母直接追加，最后递归处理剩余部分。 | 第49名 | 58次 | [`java/394.java`](java/394.java) |
| 402 | 移掉K位数字 | remove-k-digits | 栈、贪心、单调栈 | 删除数字字符串中的 k 位，使剩余数字最小。 | 栈、贪心、单调栈、去前导零 | 用双端队列 `deque` 作单调不降栈，扫描时栈顶大于当前数字且 `k>0` 则 `pollLast` 弹出；剩余 `k` 从尾部删，最后去前导零，空串返回 "0"。 | 第84名 | 36次 | [`java/402.java`](java/402.java) |
| 415 | 字符串相加 | add-strings | 字符串、模拟、进位 | 不使用大整数库，计算两个非负整数字符串之和。 | 字符串、模拟、进位、模拟加法、双指针 | 双指针 `i`、`j` 从两串末尾出发逐位相加，`add` 记进位，`ans.append(result % 10)` 追加个位，最后 `ans.reverse()` 翻转 `StringBuffer` 输出。 | 第27名 | 85次 | [`java/415.java`](java/415.java) |
| 437 | 路径总和 III | path-sum-iii | 二叉树、DFS、前缀和 | 统计二叉树中节点和等于 targetSum 的路径条数（路径不需从根开始或在叶结束，只能向下）。 | 二叉树、前缀和、前缀和 + 哈希 + 回溯 | 沿用「和为 K 的子数组」思路：DFS 维护根到当前节点的路径和 `s`，`ans += cnt.getOrDefault(s-targetSum, 0)`，`cnt` 记录祖先路径和出现次数（初始 `0:1`），递归返回时 `cnt.put(s, cnt.get(s)-1)` 回溯恢复。 | 未入Top200 | 6次 | [`java/437.java`](java/437.java) |
| 438 | 找到字符串中所有字母异位词 | find-all-anagrams-in-a-string | 字符串、定长滑动窗口 | 找出 s 中所有 p 的异位词子串的起始索引。 | 字符串、定长滑动窗口、计数数组比较 | `cntP`/`cntS` 两个 26 长度计数数组，右端 `i` 扩张计数，窗口达到 `p.length()` 时 `Arrays.equals(cntP,cntS)` 判定记录左端点 `j`，随后左端收缩计数。 | 未入Top200 | 8次 | [`java/438.java`](java/438.java) |
| 440 | 字典序的第K小数字 | k-th-smallest-in-lexicographical-order | 字典树、前缀计数、数学 | 在 1 到 n 的整数中字典序第 k 小的数字。 | 字典树、前缀计数、数学、字典序、十叉树 | 把 `[1..n]` 看作十叉前缀树，`curr` 从 1 出发，`getSteps` 计算以 `curr` 为前缀且 `<=n` 的节点数；`steps<=k` 则跳兄弟 `curr++` 并扣减 k，否则下探 `curr*=10`。 | 第79名 | 39次 | [`java/440.java`](java/440.java) |
| 470 | 用 Rand7() 实现 Rand10() | implement-rand10-using-rand7 | 概率、拒绝采样、随机数 | 只调用 `rand7()` 实现均匀的 `rand10()`。 | 概率、拒绝采样、随机数、等概率、进制转换 | 两次 `rand7()` 构造 `bit2*7+bit1` 共 49 种等概率组合，拒绝采样仅保留 `[0,39]`，映射 `ans%10+1` 得到等概率 `[1,10]`。 | 第65名 | 46次 | [`java/470.java`](java/470.java) |
| 543 | 二叉树的直径 | diameter-of-binary-tree | 二叉树、DFS、树高 | 求二叉树中任意两节点路径的最大边数。 | 二叉树、DFS、树高、后序遍历、全局最大值 | DFS 后序遍历，`dfs` 返回以当前节点为端点的最大链长（空节点返回 -1），用 `left+right+2` 更新全局变量 `ans`。 | 第83名 | 36次 | [`java/543.java`](java/543.java) |
| 560 | 和为K的子数组 | subarray-sum-equals-k | 数组、前缀和、哈希表 | 统计和等于 k 的连续子数组个数。 | 数组、前缀和、哈希表、两数之差 | 用哈希表 `preToCount` 记录前缀和出现次数（初始放入 `0:1`），遍历时查 `pre-k` 是否存在，存在则累加其计数到 `ans`。 | 第97名 | 28次 | [`java/560.java`](java/560.java) |
| 662 | 二叉树最大宽度 | maximum-width-of-binary-tree | 二叉树、BFS、节点编号 | 计算二叉树所有层中的最大宽度。 | 二叉树、BFS、节点编号、层序遍历 | BFS 层序遍历，用 `Pair<TreeNode, Integer>` 给节点编号（左子 `2*nodeNum`，右子 `2*nodeNum+1`），每层宽度为首尾编号差 `cur.last - cur.first + 1`，取最大值。 | 第51名 | 54次 | [`java/662.java`](java/662.java) |
| 695 | 岛屿的最大面积 | max-area-of-island | 网格、DFS、连通块面积 | 求网格中由 1 组成的最大岛屿面积。 | 网格、DFS、连通块面积、连通分量、原地标记 | 遍历矩阵遇 `grid[i][j]==1` 调 `dfs` 求面积，访问过的格子置 `-1` 原地标记，向四方向递归累加面积取最大。 | 第67名 | 46次 | [`java/695.java`](java/695.java) |
| 704 | 二分查找 | binary-search | 数组、二分查找 | 在升序数组中查找 target，返回下标或 -1。 | 数组、二分、开区间 lowerBound | `lowerBound` 开区间写法（`left=-1, right=n`，不变量 `nums[left]<target<=nums[right]`）定位第一个 `>=target` 的下标，再判断是否等于 target。 | 第109名 | 26次 | [`java/704.java`](java/704.java) |
| 718 | 最长重复子数组 | maximum-length-of-repeated-subarray | 数组、动态规划 | 求两个整数数组中公共的、长度最长的子数组长度。 | 数组、动态规划、DP、滚动数组 | `dp[i][j]` 表示 A[i:] 与 B[j:] 的最长公共前缀（即以 `A[i]`、`B[j]` 开头的最长公共子数组）：`A[i]==B[j] ? dp[i+1][j+1]+1 : 0`，从 `i=n-1` 倒序用两行滚动数组计算，取全局最大。 | 第114名 | 24次 | [`java/718.java`](java/718.java) |
| 739 | 每日温度 | daily-temperatures | 栈、单调栈 | 对每天温度求还要等几天才会升温，不存在为 0。 | 单调栈、逆序入栈 | 逆序遍历，栈中保存「下一个更大元素候选下标」，弹出所有 `<=` 当前温度的下标，栈顶即下一个更大元素，`ans[i]=stack.peek()-i`，再把当前下标入栈。 | 第107名 | 26次 | [`java/739.java`](java/739.java) |
| 912 | 排序数组 | sort-an-array | 排序、快速排序、随机 pivot、挖坑交换 | 对整数数组升序排序。（对应 Codetop 补充题4「手撕快速排序」） | 排序、快速排序、随机 pivot、挖坑交换 | `partition` 先随机选 pivot 交换到 `left`（避免有序数组退化），左右指针交替扫描交换把小于 pivot 的换到左侧，相遇处放回 pivot，递归排序两侧。 | 第42名（补充题4） | 65次 | [`java/912.java`](java/912.java) |
| 958 | 二叉树的完全性检验 | check-completeness-of-a-binary-tree | 二叉树、BFS、完全二叉树 | 判断一棵二叉树是否为完全二叉树。 | 二叉树、BFS、完全二叉树、层序遍历、节点编号 | BFS 层序遍历，用 `ANode` 包装节点与编号（根为 1，左 `code*2` 右 `code*2+1`），完全二叉树最后一个节点编号应等于节点总数 `nodes.size()`。 | 第92名 | 30次 | [`java/958.java`](java/958.java) |
| 994 | 腐烂的橘子 | rotting-oranges | 矩阵、多源BFS | 每分钟烂橘子感染四方向相邻新鲜橘子，求全部腐烂的最少分钟数，无法全烂返回 -1。 | 多源 BFS、按层扩散 | 初始把所有烂橘子坐标入队并统计新鲜数 `fresh`，每轮（一分钟）对当前队列四方向感染新鲜橘子（置 2、入下一层队列、`fresh--`），`fresh==0` 时结束；循环退出后 `fresh>0` 返回 -1。 | 未入Top200 | 2次 | [`java/994.java`](java/994.java) |
| 1143 | 最长公共子序列 | longest-common-subsequence | 字符串、动态规划、LCS | 求两个字符串的最长公共子序列长度。 | 字符串、动态规划、LCS、DP、滚动数组、最长公共子序列 | DP 用滚动数组 `dp[2][n+1]`，字符相等时 `dp[i%2][j]=dp[(i-1)%2][j-1]+1`，否则取 `dp[(i-1)%2][j]` 与 `dp[i%2][j-1]` 的最大值，返回 `dp[m%2][n]`。 | 第46名 | 60次 | [`java/1143.java`](java/1143.java) |
| 补充题1 | 排序奇升偶降链表 | https://mp.weixin.qq.com/s/0WVa2wIAeG0nYnVndZiEXQ | 链表、拆分、反转、归并 | 将奇数位升序、偶数位降序的链表整理成整体升序链表。 | 链表、拆分、反转、归并、链表拆分、归并合并 | 三步：`splitList` 按奇偶位置拆成两条链表，`reverseList` 反转偶数链表，`mergeTwoLists` 归并合并两个升序链表。 | 第68名 | 45次 | [`java/排序奇升偶降链表.java`](java/排序奇升偶降链表.java) |
| 补充题2 | 圆环回原点问题 | https://mp.weixin.qq.com/s/NZPaFsFrTybO3K3s7p7EVg | 动态规划、环、计数 | 在圆环上按步移动，计算回到原点的方案数。 | 动态规划、环、计数、滚动数组 | `dp[i][j]` 表示走 i 步到达 j 点的方案数，转移 `dp[i][j]=dp[i-1][(j-1+10)%10]+dp[i-1][(j+1)%10]`（取模处理环形越界），初始 `dp[0][0]=1`，用 `dp[2][10]` 滚动数组优化空间，返回 `dp[n%2][0]`。 | 第88名 | 35次 | [`java/圆环回原点问题.java`](java/圆环回原点问题.java) |
| 剑指 Offer 22 | 链表中倒数第k个节点 | lian-biao-zhong-dao-shu-di-kge-jie-dian-lcof | 链表、双指针、倒数节点 | 返回链表中倒数第 k 个节点。 | 链表、双指针、倒数节点、快慢指针、一次遍历 | 快指针 `post` 先走 `cnt` 步，再 `pre`、`post` 同步前进，`post` 到 `null` 时 `pre` 即倒数第 `cnt` 个节点。 | 第77名 | 40次 | [`java/链表倒数第k个节点.java`](java/链表倒数第k个节点.java) |
| 剑指 Offer 26 | 树的子结构 | shu-de-zi-jie-gou-lcof | 二叉树、递归、子结构匹配 | 判断一棵树是否包含另一棵树的子结构。 | 二叉树、递归、子结构匹配、先序遍历、结构匹配 | 对 `tree1` 每个节点调用 `dfs` 同步匹配 `tree2`，`node2==null` 视为匹配成功，`node1==null` 或 `node1.val!=node2.val` 则失败，递归比较左右子树。 | 第70名 | 44次 | [`java/树的子结构.java`](java/树的子结构.java) |
