# Codetop 公司题频 Top 100 对照表

数据源：`https://codetop.cc/api/questions/?company=1&page=1..5&search=&ordering=-frequency`，抓取日期：2026-06-10。接口默认每页 20 条，因此这里覆盖第 1-5 页共 100 条。

本地解法来源：`java/{题号}.java`。Top 100 中 87 道可匹配本地 Java 题解，13 道为未收录、补充题或剑指 Offer 题号。

| 题号 | 中文名称 | 英文名/Slug | 题目关键词 | 题目概要描述 | 本地解法关键词 | 本地解法思路描述 |
|---|---|---|---|---|---|---|
| 3 | 无重复字符的最长子串 | longest-substring-without-repeating-characters | 字符串、滑动窗口、哈希计数 | 给定字符串，求不含重复字符的最长连续子串长度。 | 滑动窗口、字符计数 | 右指针扩张窗口，`cnt[128]` 统计字符；出现重复时移动左指针直到窗口无重复。 |
| 146 | LRU缓存机制 | lru-cache | 设计、哈希表、双向链表、LRU | 实现支持 O(1) `get` 和 `put` 的最近最少使用缓存。 | LRU、哈希表、双向链表 | map 定位节点，环形 dummy 双向链表维护最近使用顺序，访问移到头部，超容量删尾部。 |
| 25 | K 个一组翻转链表 | reverse-nodes-in-k-group | 链表、分组翻转、原地操作 | 每 k 个节点一组翻转链表，不足 k 个的尾部保持原顺序。 | k 组翻转、三指针 | 先统计链表长度，每够 k 个节点就用 `pre/cur/next` 原地翻转，并用 `p0` 接回前后链表。 |
| 206 | 反转链表 | reverse-linked-list | 链表、指针反转、迭代 | 反转单链表并返回新的头节点。 | 三指针反转链表 | 用 `pre/cur/next` 原地反转 next 指针，最后返回 `pre`。 |
| 215 | 数组中的第K个最大元素 | kth-largest-element-in-an-array | 数组、快速选择、堆、排序 | 在未排序数组中返回第 k 大元素，而不是第 k 个不同元素。 | 快速选择、随机 pivot、三路划分 | 将第 k 大转成升序下标 `n-k`，随机选 pivot 后三路划分并只继续目标所在区间。 |
| 200 | 岛屿数量 | number-of-islands | 网格、DFS、BFS、连通分量 | 给定由陆地和水组成的网格，统计四方向相连的岛屿数量。 | 网格 DFS、岛屿计数 | 遍历网格，遇 `'1'` 就 DFS 标记为 `'2'` 并计数。 |
| 103 | 二叉树的锯齿形层次遍历 | binary-tree-zigzag-level-order-traversal | 二叉树、BFS、层序遍历、双端队列 | 按层遍历二叉树，层与层之间左右方向交替输出。 | BFS、双端插入、之字形 | 层序遍历时用 `leftToRight` 标记，当前层值按方向加到链表头或尾。 |
| 15 | 三数之和 | 3sum | 数组、排序、双指针、去重 | 找出数组中所有和为 0 且不重复的三元组。 | 排序、双指针、去重 | 排序后固定 `i`，在右侧用双指针找两数和，并跳过重复元素和做边界剪枝。 |
| 33 | 搜索旋转排序数组 | search-in-rotated-sorted-array | 数组、二分、旋转有序数组 | 在无重复元素的旋转升序数组中查找目标下标。 | 旋转数组二分 | 每轮判断左半或右半是否有序，再根据 target 是否落在有序区间决定收缩方向。 |
| 121 | 买卖股票的最佳时机 | best-time-to-buy-and-sell-stock | 数组、贪心、一次遍历 | 只能买卖一次股票，求可获得的最大利润。 | 一次遍历、最低买入价 | 遍历价格维护历史最低价，用当前价减最低价更新最大利润。 |
| 54 | 螺旋矩阵 | spiral-matrix | 矩阵、模拟、方向控制 | 按顺时针螺旋顺序返回矩阵所有元素。 | 模拟、方向数组、visited | 按右下左上方向走，遇边界或已访问就转向，直到访问 `m*n` 个元素。 |
| 5 | 最长回文子串 | longest-palindromic-substring | 字符串、回文、中心扩展、动态规划 | 返回字符串中最长的回文子串。 | 中心扩展、回文 | 枚举每个字符作为奇偶回文中心，向两侧扩展并维护最长区间。 |
| 236 | 二叉树的最近公共祖先 | lowest-common-ancestor-of-a-binary-tree | 二叉树、递归、最近公共祖先 | 在二叉树中找到两个指定节点的最近公共祖先。 | 后序递归、最近公共祖先 | 当前节点为 p、q 或 null 直接返回；左右递归都非空则当前为 LCA，否则返回非空侧。 |
| 42 | 接雨水 | trapping-rain-water | 数组、双指针、单调栈、前后缀最大值 | 给定柱状高度，计算雨后可以接住的水量。 | 双指针、左右最大值 | 维护 `leftMax/rightMax`，较低侧可确定接水量并向内移动。 |
| 1 | 两数之和 | two-sum | 数组、哈希表、补数 | 在数组中找出和为 target 的两个数并返回下标。 | 哈希表、补数查找 | 遍历数组时用 `Map<值, 下标>` 保存已见元素，发现 `target - nums[i]` 已存在就返回两个下标。 |
| 46 | 全排列 | permutations | 回溯、排列、数组 | 返回不含重复数字数组的所有排列。 | 回溯、排列 | DFS 维护路径，跳过已在 path 中的元素，长度等于数组长度时复制加入答案。 |
| 53 | 最大子数组和 | maximum-subarray | 数组、动态规划、前缀和、Kadane | 求连续子数组的最大和。 | 前缀和、最小前缀 | 遍历维护当前前缀和和历史最小前缀，用二者差值更新最大子数组和。 |
| 160 | 相交链表 | intersection-of-two-linked-lists | 链表、双指针、指针切换 | 返回两个单链表开始相交的节点，不相交返回 null。 | 双指针换头、链表相交 | 两指针分别走 A/B，到尾后切到另一条链表，最终在交点或 null 相遇。 |
| 23 | 合并K个排序链表 | merge-k-sorted-lists | 链表、归并、堆、分治 | 将 k 个升序链表合并为一个升序链表。 | 自底向上归并、两两合并 | 按 `step=1,2,4...` 分组合并链表数组，复用两个有序链表合并函数。 |
| 300 | 最长上升子序列 | longest-increasing-subsequence | 数组、动态规划、二分、贪心 | 求数组中最长严格递增子序列的长度。 | 贪心、二分、耐心排序 | 用数组前缀维护各长度递增子序列的最小结尾，二分替换第一个 `>= x` 的位置。 |
| 31 | 下一个排列 | next-permutation | 数组、双指针、字典序 | 将数组原地改成字典序下一个排列，不存在则变为最小排列。 | 下一个排列、后缀反转 | 从右找第一个上升拐点，再从右找更大元素交换，最后反转右侧降序后缀。 |
| 143 | 重排链表 | reorder-list | 链表、快慢指针、反转、合并 | 将链表重排为首尾交替的顺序。 | 找中点、反转后半、交错合并 | 快慢指针找中点，断开并反转后半链表，再交替合并前后两段。 |
| 88 | 合并两个有序数组 | merge-sorted-array | 数组、双指针、原地合并 | 将两个非降序数组合并到第一个数组中。 | 逆向双指针、原地合并 | 从 `nums1` 尾部填充较大值，避免覆盖前面的有效元素。 |
| 199 | 二叉树的右视图 | binary-tree-right-side-view | 二叉树、DFS、BFS、层序 | 返回从右侧能看到的二叉树节点值。 | 右优先 DFS、深度首次 | 先递归右子树，某深度第一次访问到的节点就是右视图节点。 |
| 20 | 有效的括号 | valid-parentheses | 字符串、栈、括号匹配 | 判断括号字符串是否按正确顺序闭合。 | 栈、括号匹配 | 奇数长度直接 false；右括号时检查栈顶是否为对应左括号，否则左括号入栈。 |
| 102 | 二叉树的层序遍历 | binary-tree-level-order-traversal | 二叉树、BFS、队列 | 按层返回二叉树节点值。 | BFS 层序遍历 | 队列按层弹出固定数量节点，收集当前层并入队左右孩子。 |
| 415 | 字符串相加 | add-strings | 字符串、模拟、进位 | 不使用大整数库，计算两个非负整数字符串之和。 | 字符串加法、进位 | 从两个字符串末尾逐位相加，保存进位，最后反转结果。 |
| 21 | 合并两个有序链表 | merge-two-sorted-lists | 链表、双指针、递归 | 合并两个升序链表并返回新链表头。 | 双指针、哑节点 | 用 prehead 拼接两个有序链表中较小节点，最后接上剩余链表。 |
| 72 | 编辑距离 | edit-distance | 字符串、动态规划、编辑距离 | 计算将一个单词转换为另一个单词的最少插入、删除、替换次数。 | 编辑距离、二维 DP | 初始化空串边界，逐格取删除、插入、替换或匹配三种转移最小值。 |
| 141 | 环形链表 | linked-list-cycle | 链表、快慢指针、环检测 | 判断链表中是否存在环。 | 快慢指针、环检测 | slow 每次一步、fast 每次两步，相遇表示有环，fast 到 null 表示无环。 |
| 92 | 反转链表 II | reverse-linked-list-ii | 链表、区间翻转、指针操作 | 只反转链表中从 left 到 right 的一段。 | 区间链表翻转、三指针 | 找到待翻转段前驱 `p0`，翻转 `right-left+1` 个节点后接回前后链表。 |
| 124 | 二叉树中的最大路径和 | binary-tree-maximum-path-sum | 二叉树、DFS、树形 DP | 求二叉树中任意非空路径的最大路径和。 | 树形 DP、最大路径和 | DFS 返回以当前节点向上延伸的非负最大贡献，同时用 `left+right+val` 更新全局答案。 |
| 56 | 合并区间 | merge-intervals | 数组、排序、区间合并 | 合并所有重叠区间。 | 排序、区间合并 | 按左端点排序，若当前区间与结果末尾重叠则更新右端点，否则追加。 |
| 41 | 缺失的第一个正数 | first-missing-positive | 数组、原地哈希、置换 | 用 O(n) 时间和 O(1) 额外空间找缺失的最小正整数。 | 原地哈希、位置交换 | 将值 `x` 尽量交换到下标 `x-1`，再找第一个 `nums[i] != i+1` 的位置。 |
| 165 | 比较版本号 | compare-version-numbers | 字符串、双指针、模拟 | 比较两个版本号字符串的大小。 | 版本分段比较 | 按 `.` 分割版本号，逐段转整数比较，缺失段按 0。 |
| 221 | 最大正方形 | maximal-square | 矩阵、动态规划 | 在 0/1 矩阵中找只包含 1 的最大正方形面积。 | 二维 DP、最大正方形 | `dp[i][j]` 表示以该格为右下角的最大边长，取左、上、左上最小值加一。 |
| 148 | 排序链表 | sort-list | 链表、排序、归并 | 在 O(n log n) 时间内对链表排序。 | 链表转数组、排序重连 | 收集所有链表节点到 List，按 `val` 排序后重新串成链表。 |
| 32 | 最长有效括号 | longest-valid-parentheses | 字符串、栈、动态规划 | 求最长合法括号子串长度。 | 栈、下标边界 | 栈存括号下标，栈底保留最后一个未匹配右括号下标，用当前下标减栈顶更新长度。 |
| 69 | x 的平方根 | sqrtx | 数学、二分、整数运算 | 返回非负整数 x 的算术平方根整数部分。 | 数学函数、exp/log | 用 `exp(0.5 * log(x))` 估算平方根，再校正 `ans+1` 是否仍不超过 x。 |
| 4 | 寻找两个正序数组的中位数 | median-of-two-sorted-arrays | 数组、二分、分治、第 k 小 | 在两个有序数组中求整体中位数，要求 O(log(m+n))。 | 递归找第 k 小、二分淘汰 | 将中位数转成两个第 k 小查询，递归比较两数组第 `k/2` 位置并淘汰较小一侧。 |
| 22 | 括号生成 | generate-parentheses | 回溯、递归、卡特兰数 | 生成 n 对括号的所有合法组合。 | 递归生成、缓存、卡特兰结构 | `generate(n)` 枚举左侧括号对数 `c`，组合左子结果和右子结果，并用数组缓存。 |
| 补充题4 | 手撕快速排序 | sort-an-array | 排序、快速排序、分治 | 实现快速排序，对数组进行升序排序。 | 未收录 | 仓库中未找到 `java/补充题4.java` 或对应数字题号文件。 |
| 101 | 对称二叉树 | symmetric-tree | 二叉树、递归、镜像 | 判断二叉树是否关于中心轴对称。 | 镜像递归 | 递归比较两棵子树根值，并交叉检查 `a.right/b.left` 与 `a.left/b.right`。 |
| 93 | 复原IP地址 | restore-ip-addresses | 字符串、回溯、分段枚举 | 从字符串中恢复所有合法 IPv4 地址。 | 回溯、IP 分段 | DFS 尝试长度 1-3 的片段，校验无非法前导零且范围 0-255，正好四段且用完字符串时记录。 |
| 1143 | 最长公共子序列 | longest-common-subsequence | 字符串、动态规划、LCS | 求两个字符串的最长公共子序列长度。 | LCS、二维 DP | `dp[i][j]` 表示两个前缀的最长公共子序列，相等取左上加一，否则取上或左最大。 |
| 105 | 从前序与中序遍历序列构造二叉树 | construct-binary-tree-from-preorder-and-inorder-traversal | 二叉树、递归、哈希表 | 根据前序和中序遍历结果构造二叉树。 | 前序中序建树、哈希定位 | 用中序值到下标的 map 定位根节点，按左子树长度切分前序和中序区间递归建树。 |
| 322 | 零钱兑换 | coin-change | 动态规划、完全背包、记忆化搜索 | 用给定硬币凑出指定金额，求最少硬币数。 | 完全背包、记忆化搜索 | `dfs(i,c)` 表示用前 i 种硬币凑金额 c 的最少硬币数，选择不用当前或继续用当前。 |
| 129 | 求根到叶子节点数字之和 | sum-root-to-leaf-numbers | 二叉树、DFS、路径数字 | 每条根到叶路径形成一个数字，求所有数字之和。 | DFS、路径数字累积 | 递归时 `currentSum = currentSum * 10 + val`，到叶子累加到答案。 |
| 394 | 字符串解码 | decode-string | 栈、字符串、递归解析 | 解码形如 `k[encoded_string]` 的嵌套字符串。 | 栈、字符串解码 | 数字、字母、`[` 入栈；遇 `]` 弹出 encoded string 和重复次数，展开后再入栈。 |
| 39 | 组合总和 | combination-sum | 回溯、组合、剪枝 | 从无重复候选数中找所有和为 target 的组合，每个数可重复使用。 | 回溯、可重复选择 | DFS 在每个候选数上做不选和选且继续停留当前下标，剩余 target 为 0 时记录路径。 |
| 662 | 二叉树最大宽度 | maximum-width-of-binary-tree | 二叉树、BFS、节点编号 | 计算二叉树所有层中的最大宽度。 | 层序遍历、完全二叉树编号 | 每层保存节点和编号，孩子编号为 `2*i`/`2*i+1`，用该层首尾编号差计算宽度。 |
| 76 | 最小覆盖子串 | minimum-window-substring | 字符串、滑动窗口、哈希计数 | 在 s 中找包含 t 全部字符的最短子串。 | 滑动窗口、字符覆盖 | 维护 `cntS/cntT`，右扩直到覆盖，覆盖时左缩并更新最小窗口；覆盖判断遍历大小写字母。 |
| 232 | 用栈实现队列 | implement-queue-using-stacks | 栈、队列、设计 | 只用栈实现队列的 push、pop、peek、empty。 | 双栈实现队列 | push 入 `stack1`；pop/peek 时全部倒到 `stack2` 取队头，再倒回 `stack1`。 |
| 209 | 长度最小的子数组 | minimum-size-subarray-sum | 数组、滑动窗口、前缀和 | 找和至少为 target 的最短连续子数组长度。 | 滑动窗口、最短子数组 | 右指针累加，窗口和达到 target 时持续左缩并更新最短长度。 |
| 43 | 字符串相乘 | multiply-strings | 字符串、模拟、乘法 | 不用大整数库，返回两个非负整数字符串的乘积。 | 字符串乘法、逐位累加 | 逐位用 `num2` 的每一位乘 `num1`，补零后调用本地字符串加法累加。 |
| 142 | 环形链表 II | linked-list-cycle-ii | 链表、快慢指针、环入口 | 返回链表入环的第一个节点，无环返回 null。 | 列表记录访问节点 | 顺链表遍历，用 `List.contains` 判断是否已访问，首次重复节点即环入口。 |
| 162 | 寻找峰值 | find-peak-element | 数组、二分、峰值 | 找到任意一个严格大于相邻元素的峰值下标。 | 二分、峰值方向 | 比较 `nums[mid]` 与 `nums[mid+1]`，上升则峰在右侧，否则在左侧含 mid。 |
| 78 | 子集 | subsets | 回溯、位运算、子集枚举 | 返回不含重复元素数组的所有子集。 | 子集回溯、选/不选 | DFS 对每个元素递归不选和选，到末尾复制路径。 |
| 2 | 两数相加 | add-two-numbers | 链表、模拟、进位 | 两个逆序链表表示非负整数，返回它们的和。 | 链表模拟加法、进位 | 用 dummy/rear 构造结果链表，分别处理两链表同步段和剩余段，最后补上进位节点。 |
| 19 | 删除链表的倒数第N个节点 | remove-nth-node-from-end-of-list | 链表、双指针、哑节点 | 删除链表倒数第 n 个节点并返回头节点。 | 未收录 | 仓库中未找到 `java/19.java`。 |
| 122 | 买卖股票的最佳时机 II | best-time-to-buy-and-sell-stock-ii | 数组、贪心、动态规划 | 可多次买卖股票，求最大利润。 | 股票 DP、贪心正收益 | 文件包含 DP 持股/不持股状态，也有累加相邻正差值的贪心版本。 |
| 82 | 删除排序链表中的重复元素 II | remove-duplicates-from-sorted-list-ii | 链表、哑节点、去重 | 删除有序链表中所有重复值节点，只保留未重复的值。 | 链表去重、哑节点 | 用 `cur` 查看后两个节点，发现重复值就循环删除所有该值节点。 |
| 239 | 滑动窗口最大值 | sliding-window-maximum | 队列、单调队列、滑动窗口 | 返回每个长度为 k 的滑动窗口中的最大值。 | 单调队列、滑动窗口最大值 | 队列存下标并保持对应值递减，移除过期下标，窗口形成后取队首值。 |
| 112 | 路径总和 | path-sum | 二叉树、DFS、路径和 | 判断树中是否存在根到叶路径，其节点和等于 target。 | 根到叶路径和、递归扣减 | 每访问节点从 target 中减去节点值，叶子节点时判断剩余是否为 0。 |
| 98 | 验证二叉搜索树 | validate-binary-search-tree | 二叉树、BST、递归、中序遍历 | 判断二叉树是否为合法二叉搜索树。 | 递归校验、子树全量比较 | 对每个节点检查左子树所有值小于根、右子树所有值大于根，再递归左右子树。 |
| 695 | 岛屿的最大面积 | max-area-of-island | 网格、DFS、连通块面积 | 求网格中由 1 组成的最大岛屿面积。 | 网格 DFS、岛屿面积 | 遍历陆地 1，DFS 四方向累加面积并把访问过的陆地改为 -1。 |
| 470 | 用 Rand7() 实现 Rand10() | implement-rand10-using-rand7 | 概率、拒绝采样、随机数 | 只调用 `rand7()` 实现均匀的 `rand10()`。 | 拒绝采样、rand7 生成 rand10 | 两次 `rand7()-1` 生成 0-48 均匀数，只接受 0-39 并映射到 1-10。 |
| 补充题1 | 排序奇升偶降链表 | https://mp.weixin.qq.com/s/0WVa2wIAeG0nYnVndZiEXQ | 链表、拆分、反转、归并 | 将奇数位升序、偶数位降序的链表整理成整体升序链表。 | 未收录 | 仓库中未找到对应补充题文件。 |
| 94 | 二叉树的中序遍历 | binary-tree-inorder-traversal | 二叉树、DFS、中序遍历 | 返回二叉树的中序遍历结果。 | 递归中序遍历 | 递归访问左子树、当前节点、右子树并收集值。 |
| 240 | 搜索二维矩阵 II | search-a-2d-matrix-ii | 矩阵、二分、搜索剪枝 | 在每行每列均升序的矩阵中查找 target。 | 右上角搜索、矩阵剪枝 | 从右上角开始，当前值大于 target 左移，小于 target 下移。 |
| 155 | 最小栈 | min-stack | 栈、设计、辅助栈 | 设计支持常数时间获取最小值的栈。 | 双栈、最小栈 | 普通栈存值，辅助栈每层存当前最小值，push/pop 同步维护。 |
| 139 | 单词拆分 | word-break | 字符串、动态规划、Trie | 判断字符串是否能由字典中的单词拼接而成。 | 未收录 | 仓库中未找到 `java/139.java`。 |
| 剑指 Offer 26 | 树的子结构 | shu-de-zi-jie-gou-lcof | 二叉树、递归、子结构匹配 | 判断一棵树是否包含另一棵树的子结构。 | 未收录 | 仓库中未找到剑指 Offer 26 对应题解文件。 |
| 79 | 单词搜索 | word-search | 回溯、矩阵、DFS | 判断单词是否能由网格中相邻字符路径组成，每格最多用一次。 | 网格 DFS、回溯 visited | 从首字母匹配位置开始四方向 DFS，使用 `visited` 防止同路径重复访问，回溯恢复。 |
| 64 | 最小路径和 | minimum-path-sum | 矩阵、动态规划、记忆化搜索 | 从左上到右下，只能向右或向下，求最小路径和。 | 记忆化搜索、最小路径和 | 从右下角递归向上或向左求最小路径和，用 `memo` 缓存，越界返回极大值。 |
| 113 | 路径总和 II | path-sum-ii | 二叉树、DFS、回溯、路径和 | 返回所有根到叶路径和等于 target 的路径。 | DFS 回溯、路径复制 | 维护当前路径和剩余值，叶子且剩余为 0 时复制路径，返回前移除当前节点。 |
| 128 | 最长连续序列 | longest-consecutive-sequence | 哈希表、数组、连续序列 | 在未排序数组中求最长连续整数序列长度。 | HashSet、序列起点扩展 | 用 set 去重，只从不存在 `x-1` 的数开始向后扩展连续长度。 |
| 152 | 乘积最大子数组 | maximum-product-subarray | 数组、动态规划、最大最小状态 | 求连续子数组的最大乘积。 | 最大/最小乘积 DP | 同时维护以当前位置结尾的最大和最小乘积，处理负数翻转，文件含数组版和 O(1) 版。 |
| 剑指 Offer 22 | 链表中倒数第k个节点 | lian-biao-zhong-dao-shu-di-kge-jie-dian-lcof | 链表、双指针、倒数节点 | 返回链表中倒数第 k 个节点。 | 未收录 | 仓库中未找到剑指 Offer 22 对应题解文件。 |
| 440 | 字典序的第K小数字 | k-th-smallest-in-lexicographical-order | 字典树、前缀计数、数学 | 在 1 到 n 的整数中字典序第 k 小的数字。 | 未收录 | 仓库中未找到 `java/440.java`。 |
| 48 | 旋转图像 | rotate-image | 矩阵、原地旋转、转置 | 将 n x n 矩阵原地顺时针旋转 90 度。 | 矩阵翻转、原地旋转 | 先按水平轴上下翻转，再沿主对角线转置，实现顺时针旋转 90 度。 |
| 70 | 爬楼梯 | climbing-stairs | 动态规划、斐波那契 | 每次爬 1 或 2 阶，求到第 n 阶的方法数。 | 动态规划、斐波那契 | `dp[i]=dp[i-1]+dp[i-2]`，前两项初始化为 1、2。 |
| 402 | 移掉K位数字 | remove-k-digits | 栈、贪心、单调栈 | 删除数字字符串中的 k 位，使剩余数字最小。 | 未收录 | 仓库中未找到 `java/402.java`。 |
| 198 | 打家劫舍 | house-robber | 动态规划、滚动数组 | 相邻房屋不能同时偷，求可偷的最大金额。 | 打家劫舍 DP、记忆化/滚动 | 文件包含记忆化递归、数组 DP 和 O(1) 滚动变量，转移为偷当前加 `i-2` 或不偷取 `i-1`。 |
| 62 | 不同路径 | unique-paths | 动态规划、组合数学、网格路径 | 机器人从左上到右下，只能向右或向下，求路径数。 | 记忆化搜索、DP、滚动数组 | 文件中包含多版：递归记忆化 `dfs(i,j)`，二维 DP，以及一维滚动数组转移 `f[j+1]+=f[j]`。 |
| 543 | 二叉树的直径 | diameter-of-binary-tree | 二叉树、DFS、树高 | 求二叉树中任意两节点路径的最大边数。 | 树高 DFS、直径 | DFS 返回当前节点向下最大链长，用左右链长之和加 2 更新直径。 |
| 补充题23 | 检测循环依赖 | https://mp.weixin.qq.com/s/pCRscwKqQdYYN7M1Sia7xA | 图、拓扑排序、环检测 | 在依赖关系中检测是否存在循环依赖。 | 未收录 | 仓库中未找到对应补充题文件。 |
| 补充题2 | 圆环回原点问题 | https://mp.weixin.qq.com/s/NZPaFsFrTybO3K3s7p7EVg | 动态规划、环、计数 | 在圆环上按步移动，计算回到原点的方案数。 | 未收录 | 仓库中未找到对应补充题文件。 |
| 207 | 课程表 | course-schedule | 图、DFS、拓扑排序、环检测 | 判断是否可以完成所有课程，即先修依赖图是否无环。 | 图 DFS、三色标记判环 | 构建课程邻接表，0/1/2 三色 DFS，遇到正在访问的节点表示有环。 |
| 179 | 最大数 | largest-number | 排序、贪心、自定义比较 | 将非负整数数组排列成能组成的最大数字字符串。 | 自定义排序、字符串拼接 | 将数字转字符串，按 `(b+a).compareTo(a+b)` 排序，处理全 0 后拼接。 |
| 24 | 两两交换链表中的节点 | swap-nodes-in-pairs | 链表、递归、成对交换 | 两两交换链表相邻节点并返回新头。 | 数组存链表节点、成对交换 | 先把链表节点放入 `List`，每两个节点用 `Collections.swap` 交换，再重新串链。 |
| 958 | 二叉树的完全性检验 | check-completeness-of-a-binary-tree | 二叉树、BFS、完全二叉树 | 判断一棵二叉树是否为完全二叉树。 | 未收录 | 仓库中未找到 `java/958.java`。 |
| 329 | 矩阵中的最长递增路径 | longest-increasing-path-in-a-matrix | 矩阵、DFS、记忆化搜索、拓扑排序 | 求矩阵中四方向移动形成的最长严格递增路径长度。 | 未收录 | 仓库中未找到 `java/329.java`。 |
| 8 | 字符串转换整数 (atoi) | string-to-integer-atoi | 字符串、模拟、边界处理 | 按 atoi 规则将字符串转换为 32 位有符号整数。 | 正则提取、溢出钳制 | 用正则匹配前导空格后的有符号整数，`Long.parseLong` 后按 int 边界截断，过长异常按符号返回边界。 |
| 55 | 跳跃游戏 | jump-game | 数组、贪心、最远可达 | 判断是否能从数组起点跳到最后一个下标。 | 贪心、最远可达 | 遍历可达位置并更新 `rightmost`，能到末尾提前返回 true，遇不可达则最终 false。 |
| 135 | 分发糖果 | candy | 贪心、两次扫描、数组 | 按评分给孩子分糖，评分更高的相邻孩子糖更多，求最少糖数。 | 两次扫描、左右规则 | 左到右算递增需求，右到左算递减需求，每人取左右需求最大值累加。 |
| 560 | 和为K的子数组 | subarray-sum-equals-k | 数组、前缀和、哈希表 | 统计和等于 k 的连续子数组个数。 | 前缀和、哈希计数 | 遍历累加前缀和 `pre`，把已出现的 `pre-k` 次数加入答案，再记录当前 `pre`。 |
| 151 | 翻转字符串里的单词 | reverse-words-in-a-string | 字符串、双指针、分词 | 去掉多余空格并反转字符串中单词顺序。 | trim、正则 split、逆序拼接 | 去首尾空格后按连续空白分词，再从后往前拼接单词。 |
| 224 | 基本计算器 | basic-calculator | 栈、递归、表达式求值 | 计算包含加减和括号的表达式字符串。 | 未收录 | 仓库中未找到 `java/224.java`。 |
| 110 | 平衡二叉树 | balanced-binary-tree | 二叉树、DFS、树高 | 判断二叉树任意节点左右子树高度差是否不超过 1。 | 递归树高、平衡判断 | 对每个节点计算左右高度差是否不超过 1，并递归检查左右子树。 |
